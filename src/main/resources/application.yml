# 콘솔 컬러
spring.output.ansi.enabled: always

# 톰캣 서버포트
server:
  port: 8082

# database 연동 설정
spring:

  # mail
  mail:
    host: smtp.gmail.com
    port: 587
    username: dlalswo656@gmail.com # 발신자 메일
    password: zcvumzdippglzmfu # 구글 2단계 앱 비밀번호
    properties:
      mail:
        smtp:
          aurh: true
          timeout: 5000
          starttls:
            enabel: true


  # mysql 연동
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    #    각자 PC에 만들어놓은 Database이름을 써야 합니다.
    url: jdbc:mysql://localhost:3306/ms?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    #    mysql에 생성한 사용자 계정 정보를 써야 합니다.
    username: musicshare
    password: 1234
  
  # thymeleaf
  thymeleaf:
    cache: false

  # spring data jpa 설정
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: update #create